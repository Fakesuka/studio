'use client';

// Header is now minimal - no elements needed
// Bell notification moved to bottom nav with pulsing effect

export function Header() {
  // Return null - no header needed
  return null;
}
