# Бесплатный деплой YakGo для тестирования

Полное руководство по развертыванию YakGo на **100% бесплатных** сервисах для тестирования и демонстрации проекта.

## 🎯 Что мы развернем бесплатно

- ✅ **Frontend** (Next.js) → Vercel (бесплатно навсегда)
- ✅ **Backend** (Node.js + Express) → Render.com (бесплатно с ограничениями)
- ✅ **Database** (PostgreSQL) → Supabase (бесплатно, 500 MB)
- ✅ **SSL сертификаты** (автоматически)
- ✅ **Домены** (автоматические поддомены)

## 📊 Ограничения бесплатных планов

| Сервис | План | Ограничения |
|--------|------|-------------|
| Vercel | Hobby (Free) | 100 GB bandwidth/месяц, 100 деплоев/день |
| Render.com | Free | Засыпает после 15 мин простоя, 750 часов/месяц |
| Supabase | Free | 500 MB БД, 2 GB transfer, 1 GB файлового хранилища |

**Идеально для:** Тестирования, демо, MVP, небольших проектов (до 100-200 пользователей)

---

## Вариант 1: Vercel + Render + Supabase (Рекомендуется)

### Шаг 1: База данных PostgreSQL (Supabase)

#### 1.1 Создание проекта в Supabase

1. Перейдите на https://supabase.com/
2. Нажмите **Start your project** → Sign in with GitHub
3. Создайте новую организацию (или используйте существующую)
4. Нажмите **New project**

**Настройки:**
- **Name:** `yakgo-db`
- **Database Password:** Сгенерируйте надежный пароль (сохраните его!)
- **Region:** `Southeast Asia (Singapore)` (ближайший к России)
- **Pricing Plan:** `Free` (выбрано по умолчанию)

5. Нажмите **Create new project** (создание займет 2-3 минуты)

#### 1.2 Получение строки подключения

После создания проекта:

1. В боковом меню нажмите **Settings** (⚙️ внизу)
2. Выберите **Database**
3. Прокрутите до раздела **Connection string**
4. Выберите **URI** и скопируйте строку подключения

Строка будет выглядеть так:
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxxxxxxxx.supabase.co:5432/postgres
```

**ВАЖНО:** Замените `[YOUR-PASSWORD]` на пароль, который вы создали!

#### 1.3 Применение миграций к базе данных

```bash
# На локальной машине
cd ~/studio/backend

# Создайте .env для подключения к Supabase
cat > .env << 'EOF'
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxxxxxxxxxxx.supabase.co:5432/postgres"
EOF

# Замените YOUR_PASSWORD и хост на ваши реальные данные
nano .env

# Установите зависимости (если еще не установлены)
npm install

# Примените миграции
npm run prisma:push

# ✅ Вы увидите сообщение: "Your database is now in sync with your Prisma schema"

# Заполните тестовыми данными (опционально)
npm run prisma:seed
```

#### 1.4 Проверка базы данных

1. В Supabase откройте **Table Editor** (слева)
2. Вы увидите все таблицы: `User`, `Order`, `Shop`, `Product`, `Message`, `Promocode`, и т.д.

---

### Шаг 2: Backend API (Render.com)

#### 2.1 Подготовка репозитория

```bash
# Убедитесь, что все изменения закоммичены
cd ~/studio
git status

# Если есть незакоммиченные изменения
git add .
git commit -m "Prepare for deployment"
git push origin main
```

#### 2.2 Создание Web Service на Render

1. Перейдите на https://render.com/
2. Нажмите **Get Started** → Sign in with GitHub
3. Нажмите **New +** → **Web Service**
4. Подключите ваш GitHub репозиторий `studio`
5. Нажмите **Connect** рядом с репозиторием

**Настройки Web Service:**

- **Name:** `yakgo-backend` (будет доступен как `yakgo-backend.onrender.com`)
- **Region:** `Singapore` (ближайший к России)
- **Branch:** `main`
- **Root Directory:** `backend` ⚠️ **ВАЖНО!**
- **Runtime:** `Node`
- **Build Command:** `npm install && npm run build && npx prisma generate`
- **Start Command:** `npm start`
- **Instance Type:** `Free`

#### 2.3 Настройка переменных окружения

В разделе **Environment** добавьте переменные:

```env
# Database (из Supabase)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxxxxxxxxxxxx.supabase.co:5432/postgres

# Server
PORT=3001
NODE_ENV=production

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET=ваш_супер_секретный_ключ_32_символа_минимум

# Telegram Bot (создадим позже, пока оставьте заглушку)
TELEGRAM_BOT_TOKEN=temp_token
TELEGRAM_BOT_SECRET=temp_secret

# Frontend URL (обновим после деплоя frontend)
FRONTEND_URL=https://temp-url.vercel.app

# ЮKassa (если есть, иначе оставьте пустым)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
```

**Как сгенерировать JWT_SECRET:**
```bash
# На локальной машине
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Скопируйте результат в JWT_SECRET
```

#### 2.4 Деплой Backend

1. Нажмите **Create Web Service**
2. Render начнет автоматический деплой (займет 5-10 минут)
3. Следите за логами в реальном времени
4. После завершения увидите сообщение: **Deploy live** ✅

**URL вашего Backend:** `https://yakgo-backend.onrender.com`

#### 2.5 Проверка работы Backend

```bash
# Проверьте health endpoint
curl https://yakgo-backend.onrender.com/health

# Или откройте в браузере:
# https://yakgo-backend.onrender.com/health
```

⚠️ **Важно:** Бесплатный план Render засыпает после 15 минут простоя. Первый запрос после сна может занять 30-60 секунд.

---

### Шаг 3: Frontend (Vercel)

#### 3.1 Деплой на Vercel

1. Перейдите на https://vercel.com/
2. Нажмите **Sign Up** → Continue with GitHub
3. Нажмите **Add New...** → **Project**
4. Выберите репозиторий `studio` → **Import**

**Настройки проекта:**

- **Project Name:** `yakgo` (будет доступен как `yakgo.vercel.app`)
- **Framework Preset:** `Next.js` (автоматически определится)
- **Root Directory:** `./ ` (корень проекта, не `backend/`)
- **Build Command:** `npm run build` (автоматически)
- **Output Directory:** `.next` (автоматически)

#### 3.2 Переменные окружения

В разделе **Environment Variables** добавьте:

```env
# Backend API (замените на ваш URL с Render)
NEXT_PUBLIC_API_URL=https://yakgo-backend.onrender.com/api
NEXT_PUBLIC_WS_URL=https://yakgo-backend.onrender.com

# 2GIS API Key (получите на https://dev.2gis.ru/)
NEXT_PUBLIC_2GIS_API_KEY=ваш_ключ_2gis

# AI (опционально, можно оставить пустым)
OLLAMA_ENABLED=false
```

**Как получить 2GIS API Key:**
1. Перейдите на https://dev.2gis.ru/
2. Зарегистрируйтесь
3. Создайте новый проект → получите API ключ

#### 3.3 Деплой

1. Нажмите **Deploy**
2. Vercel автоматически соберет и задеплоит проект (3-5 минут)
3. После завершения получите URL: `https://yakgo.vercel.app`

#### 3.4 Обновите FRONTEND_URL в Backend

1. Вернитесь в Render.com → ваш `yakgo-backend` сервис
2. Перейдите в **Environment**
3. Измените `FRONTEND_URL` на ваш Vercel URL:
   ```
   FRONTEND_URL=https://yakgo.vercel.app
   ```
4. Нажмите **Save Changes**
5. Render автоматически передеплоит backend

---

### Шаг 4: Настройка Telegram Bot

#### 4.1 Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. **Имя бота:** `YakGo Test` (или любое другое)
4. **Username:** `YakGoTestBot` (должен быть уникальным, попробуйте разные варианты)
5. **Сохраните токен!** Будет выглядеть так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

#### 4.2 Настройка Web App

Отправьте BotFather следующие команды:

```
/setdescription
# Выберите вашего бота
# Введите описание:
Сервис помощи на дорогах Якутии. Отогрев авто, доставка топлива, эвакуатор и маркетплейс автотоваров.

/setabouttext
# Введите:
YakGo - ваш надежный помощник на дорогах

/mybots
# Выберите вашего бота → Edit Bot → Edit Commands
# Вставьте:
start - Запустить приложение
help - Помощь
```

#### 4.3 Настройка Mini App

```
# Отправьте BotFather:
/newapp

# Выберите вашего бота
# Введите название: YakGo
# Введите описание: Помощь на дорогах Якутии
# Загрузите фото (квадратное, 640x640 px минимум)
# Short name: yakgo
# Web App URL: https://yakgo.vercel.app
```

#### 4.4 Обновите токен в Backend

1. Render.com → `yakgo-backend` → **Environment**
2. Обновите переменную:
   ```
   TELEGRAM_BOT_TOKEN=ваш_реальный_токен_от_BotFather
   ```
3. Сгенерируйте секрет:
   ```bash
   node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
   ```
4. Обновите:
   ```
   TELEGRAM_BOT_SECRET=сгенерированный_секрет
   ```
5. Сохраните → Backend автоматически перезапустится

---

### Шаг 5: Проверка работы приложения

#### 5.1 Откройте Mini App в Telegram

1. Найдите вашего бота в Telegram: `@YakGoTestBot`
2. Нажмите **Start** или отправьте `/start`
3. Нажмите на кнопку меню (☰) → запустится ваше приложение!

**Или используйте прямую ссылку:**
```
https://t.me/YakGoTestBot/yakgo
```

#### 5.2 Тестирование функций

✅ **Проверьте:**
- [ ] Регистрация через Telegram (автоматическая)
- [ ] Создание заказа на услугу
- [ ] AI диагностика (если настроили)
- [ ] Маркетплейс (просмотр товаров)
- [ ] Профиль пользователя
- [ ] Регистрация водителя/продавца
- [ ] Чат (если есть активный заказ)
- [ ] Уведомления в Telegram

---

## Вариант 2: Railway (всё в одном)

Railway предоставляет **$5 бесплатно** в месяц для тестирования.

### Шаг 1: Создание проекта в Railway

1. Перейдите на https://railway.app/
2. **Sign up** → **Login with GitHub**
3. Нажмите **New Project**

### Шаг 2: Создание PostgreSQL базы

1. **New Project** → **Provision PostgreSQL**
2. Railway автоматически создаст базу данных
3. Нажмите на базу → вкладка **Variables**
4. Скопируйте `DATABASE_URL`

### Шаг 3: Деплой Backend

1. В том же проекте нажмите **New** → **GitHub Repo**
2. Выберите репозиторий `studio`
3. После импорта нажмите на сервис → **Settings**
4. **Root Directory:** `backend`
5. **Build Command:** `npm install && npm run build && npx prisma generate`
6. **Start Command:** `npm start`

### Шаг 4: Настройка переменных окружения

1. Вкладка **Variables** в backend сервисе
2. Добавьте переменные:

```env
DATABASE_URL=${{Postgres.DATABASE_URL}}  # Railway автоматически подставит
PORT=3001
NODE_ENV=production
JWT_SECRET=ваш_секрет
TELEGRAM_BOT_TOKEN=ваш_токен
FRONTEND_URL=https://yakgo.vercel.app
```

### Шаг 5: Deploy Frontend на Vercel

Повторите Шаг 3 из Варианта 1 (Vercel остается бесплатным).

**Railway стоимость:** ~$3-4/месяц при активном использовании, $5 бесплатно покрывает тесты.

---

## Мониторинг и логи

### Render.com

1. Откройте ваш сервис `yakgo-backend`
2. Вкладка **Logs** - логи в реальном времени
3. **Metrics** - использование CPU, RAM, запросы

### Supabase

1. **Database** → **Logs** - SQL запросы
2. **Database** → **Reports** - использование хранилища
3. **API** → **Logs** - API запросы (если используете Supabase API)

### Vercel

1. Откройте проект `yakgo`
2. **Deployments** → последний деплой → **View Function Logs**
3. **Analytics** - посетители, производительность (платная функция)

---

## Ограничения бесплатных планов

### Render.com Free

**Ограничения:**
- ⏰ Сервис засыпает после 15 минут неактивности
- 🚀 Холодный старт: 30-60 секунд при первом запросе
- 💾 512 MB RAM
- 🕐 750 часов работы в месяц
- ❌ Нет автоматического SSL для кастомных доменов (только поддомены .onrender.com)

**Обход проблемы засыпания:**
```bash
# Используйте бесплатный uptime мониторинг
# Например, https://uptimerobot.com/
# Настройте пинг каждые 5 минут на https://yakgo-backend.onrender.com/health
```

### Supabase Free

**Ограничения:**
- 💾 500 MB хранилища
- 🔄 2 GB bandwidth в месяц
- 👥 Неограниченное количество запросов
- ⏸️ Проект паузируется после 7 дней неактивности (легко возобновить)

**Для продакшена:** Upgrade до Pro ($25/месяц) для 8 GB хранилища и отсутствия пауз.

### Vercel Free (Hobby)

**Ограничения:**
- 🌐 100 GB bandwidth в месяц
- 🚀 100 деплоев в день
- ⚡ Отличная производительность
- ✅ Автоматический SSL
- ✅ Нет засыпания!

**Очень щедрый план, подходит для продакшена небольших проектов.**

---

## Автоматические обновления (CI/CD)

### Render.com

- ✅ **Auto-Deploy:** При push в `main` ветку GitHub автоматически деплоится
- Настройка: **Settings** → **Build & Deploy** → **Auto-Deploy: Yes**

### Vercel

- ✅ **Auto-Deploy:** Каждый push в любую ветку создает preview деплой
- `main` ветка → production
- Другие ветки → preview URL

**Пример workflow:**
```bash
# Разработка
git checkout -b feature/new-feature
git commit -m "Add new feature"
git push origin feature/new-feature

# Vercel создаст preview: https://yakgo-git-feature-new-feature.vercel.app

# После тестирования:
git checkout main
git merge feature/new-feature
git push origin main

# Vercel задеплоит на production: https://yakgo.vercel.app
```

---

## Обновление приложения

### Backend (Render)

```bash
# Локально
cd ~/studio
git add .
git commit -m "Update backend"
git push origin main

# Render автоматически:
# 1. Обнаружит изменения в GitHub
# 2. Запустит новый деплой
# 3. Применит миграции (если изменилась схема Prisma)
# 4. Перезапустит сервис
```

**Применение новых миграций БД:**

Если вы изменили `backend/prisma/schema.prisma`:

```bash
# Локально подключитесь к Supabase БД
cd backend
# Убедитесь, что DATABASE_URL в .env указывает на Supabase
npm run prisma:push

# Render автоматически выполнит это при деплое благодаря команде в Build Command
```

### Frontend (Vercel)

```bash
# Просто запушьте изменения
cd ~/studio
git add .
git commit -m "Update frontend"
git push origin main

# Vercel автоматически соберет и задеплоит за 2-3 минуты
```

---

## Кастомные домены (опционально)

### Для Frontend (Vercel)

1. Купите домен (например, на https://reg.ru/, https://timeweb.com/)
2. В Vercel проекте: **Settings** → **Domains**
3. Добавьте домен: `yakgo.ru` или `app.yakgo.ru`
4. Настройте DNS записи у регистратора:

```
Type: CNAME
Name: app (или @)
Value: cname.vercel-dns.com
TTL: 3600
```

5. Vercel автоматически выпустит SSL сертификат

### Для Backend (Render)

**⚠️ Внимание:** Бесплатный план Render **НЕ** поддерживает кастомные домены со SSL.

**Варианты:**
1. Используйте стандартный домен: `yakgo-backend.onrender.com`
2. Upgrade до платного плана ($7/месяц) для кастомных доменов
3. Используйте Cloudflare в качестве reverse proxy (бесплатно):
   - Настройте Cloudflare перед Render
   - Получите бесплатный SSL от Cloudflare

---

## Мониторинг стоимости

### Бесплатные лимиты

**Vercel Hobby (навсегда бесплатно):**
- ✅ 100 GB bandwidth/месяц
- 📊 Проверка: Vercel Dashboard → Usage

**Render Free:**
- ✅ 750 часов/месяц (достаточно для одного сервиса 24/7)
- 📊 Проверка: Render Dashboard → Account → Usage

**Supabase Free:**
- ✅ 500 MB БД
- ✅ 2 GB bandwidth
- 📊 Проверка: Supabase Dashboard → Settings → Usage

### Если превысите лимиты

- **Vercel:** Попросит upgrade ($20/месяц)
- **Render:** Сервис остановится до следующего месяца
- **Supabase:** База станет read-only до upgrade

---

## Troubleshooting

### Backend не отвечает (Render)

**Причина:** Сервис заснул (15 минут простоя)

**Решение:**
```bash
# Просто откройте URL в браузере
https://yakgo-backend.onrender.com/health

# Подождите 30-60 секунд - сервис проснется
```

**Автоматизация:**
- Используйте https://uptimerobot.com/ (бесплатно)
- Настройте проверку каждые 5 минут

### База данных недоступна

```bash
# Проверьте URL подключения
# В Supabase: Settings → Database → Connection string

# Проверьте подключение локально
psql "postgresql://postgres:PASSWORD@db.xxx.supabase.co:5432/postgres"
```

### WebSocket не работает

Render.com поддерживает WebSocket на бесплатном плане! ✅

Проверьте:
1. Backend логи: Render → Logs
2. Frontend переменные: `NEXT_PUBLIC_WS_URL` правильный
3. CORS настройки в backend (`backend/src/index.ts`)

### Telegram Bot не отвечает

1. Проверьте `TELEGRAM_BOT_TOKEN` в Render Environment Variables
2. Убедитесь, что пользователи запустили бота (`/start`)
3. Проверьте логи Backend при отправке уведомления

---

## Переход на продакшен

Когда приложение готово к реальным пользователям:

### Upgrade Backend

**Railway ($7-10/месяц):**
- ✅ Нет засыпания
- ✅ Больше ресурсов
- ✅ Автоматическое масштабирование

**Render Starter ($7/месяц):**
- ✅ Нет засыпания
- ✅ Кастомные домены с SSL
- ✅ 512 MB RAM

**Яндекс.Облако:**
- См. [DEPLOYMENT_YANDEX.md](./DEPLOYMENT_YANDEX.md)
- ~5000₽/месяц (~$55)

### Upgrade Database

**Supabase Pro ($25/месяц):**
- 8 GB хранилища
- 50 GB bandwidth
- Нет пауз
- Дневные бэкапы

**Render PostgreSQL ($7/месяц):**
- 1 GB хранилища
- Автоматические бэкапы
- Managed updates

### Frontend

Vercel Hobby достаточно для большинства проектов! При необходимости:

**Vercel Pro ($20/месяц):**
- 1 TB bandwidth
- Analytics
- Приоритетная поддержка

---

## Чек-лист деплоя

- [ ] ✅ База данных создана в Supabase
- [ ] ✅ Миграции применены (`npm run prisma:push`)
- [ ] ✅ Backend задеплоен на Render
- [ ] ✅ Frontend задеплоен на Vercel
- [ ] ✅ Telegram Bot создан через BotFather
- [ ] ✅ Mini App настроен и привязан к боту
- [ ] ✅ Переменные окружения настроены везде
- [ ] ✅ FRONTEND_URL обновлен в Backend
- [ ] ✅ Приложение открывается в Telegram
- [ ] ✅ Регистрация работает
- [ ] ✅ Создание заказов работает
- [ ] ✅ Уведомления приходят в Telegram
- [ ] ✅ WebSocket чат работает (если тестируете)

---

## Полезные ссылки

**Сервисы:**
- [Vercel Dashboard](https://vercel.com/dashboard)
- [Render Dashboard](https://dashboard.render.com/)
- [Supabase Dashboard](https://app.supabase.com/)
- [Railway Dashboard](https://railway.app/dashboard)

**Документация:**
- [Render Docs](https://render.com/docs)
- [Supabase Docs](https://supabase.com/docs)
- [Vercel Docs](https://vercel.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)

**Мониторинг:**
- [UptimeRobot](https://uptimerobot.com/) - бесплатный uptime мониторинг
- [BetterStack](https://betterstack.com/) - логи и мониторинг (бесплатный tier)

---

## Поддержка

Если что-то не работает:

1. **Проверьте логи:**
   - Backend: Render → Logs
   - Frontend: Vercel → Deployment → Function Logs
   - Database: Supabase → Logs

2. **Проверьте переменные окружения:**
   - Все ли переменные добавлены?
   - Правильные ли значения?
   - Нет ли опечаток?

3. **Создайте issue:**
   - https://github.com/Fakesuka/studio/issues

**Успешного бесплатного деплоя! 🚀**
