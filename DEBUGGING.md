# Отладка приложения YakGo

Этот документ описывает все изменения и инструкции по отладке проблем с сохранением профиля, регистрацией водителей/продавцов и запросом номера телефона.

## 🔧 Внесенные изменения

### 1. Улучшена логика получения номера телефона

**Файл:** `src/lib/telegram.ts`

- Добавлена новая функция `getTelegramPhoneNumber()` с детальным логированием
- Функция проверяет наличие номера в `initDataUnsafe.user.phone_number`
- Все этапы получения номера логируются в консоль браузера

**Файл:** `src/app/(app)/profile/page.tsx`

- Полностью переработана функция `requestPhoneNumber()`
- Добавлено логирование каждого шага процесса
- Сначала проверяется наличие номера в `initDataUnsafe`
- Если номера нет, вызывается `requestContact()` API
- Поле номера телефона теперь **read-only** (disabled) для безопасности
- Пользователь может получить номер только из Telegram

### 2. Добавлено логирование в API клиент

**Файл:** `src/lib/api.ts`

- Все запросы к API логируются: метод, endpoint, body, наличие initData
- Все ответы логируются: status, успех/ошибка, данные
- Все ошибки логируются с деталями
- Формат: `[API] Request to /users/profile`

### 3. Добавлено логирование в backend

**Файл:** `backend/src/middleware/auth.ts`

- Логирование каждого этапа аутентификации
- Проверка наличия initData
- Валидация Telegram данных
- Поиск/создание пользователя
- Формат: `[Auth] Authenticating request`

**Файл:** `backend/src/controllers/users.controller.ts` (уже было)

- Логирование запросов на обновление профиля
- Логирование регистрации водителей и продавцов
- Детали ошибок в ответах API

### 4. Улучшена загрузка данных профиля

**Файл:** `src/app/(app)/profile/page.tsx`

- Добавлено логирование загрузки профиля из API
- Логирование получения данных из Telegram
- Автоматическое заполнение телефона если он есть в Telegram
- Формат: `[Profile] Loading user data from API`

## 📊 Как проверить что работает

### 1. Открыть консоль разработчика

**В браузере:**
1. Откройте приложение в Telegram
2. Нажмите F12 или ПКМ → "Inspect" → вкладка "Console"

**В Telegram Desktop:**
1. Откройте бота в Telegram Desktop
2. Нажмите Ctrl+Shift+I (Windows/Linux) или Cmd+Option+I (Mac)
3. Перейдите на вкладку "Console"

### 2. Проверка загрузки профиля

При открытии страницы профиля вы должны увидеть:

```
[Profile] Loading user data from API
[API] Request to /users/profile { method: 'GET', hasInitData: true }
[API] Response from /users/profile: { status: 200, ok: true }
[API] Success from /users/profile { id: '...', name: '...', phone: '...', city: '...' }
[Profile] Profile loaded from API: { name: '...', phone: '...', city: '...' }
[Profile] Getting Telegram user data
[Telegram] User data: { id: 123456, first_name: '...', ... }
[Profile] User data loaded successfully
```

### 3. Проверка запроса номера телефона

При нажатии кнопки "Запросить из Telegram":

```
[Profile] Requesting phone number from Telegram
[Profile] WebApp available, checking user data
[Profile] User object: { id: 123456, first_name: '...', phone_number: '+7...' }
[Profile] Phone found in user data: +7...
```

Или если номера нет:

```
[Profile] Requesting phone number from Telegram
[Profile] WebApp available, checking user data
[Profile] User object: { id: 123456, first_name: '...', phone_number: undefined }
[Profile] Phone not in user data, requesting via requestContact
[Profile] Contact shared: true
[Profile] Updated user data: { ... }
[Profile] Phone now available: +7...
```

### 4. Проверка сохранения профиля

При нажатии "Сохранить изменения":

```
[Profile] Saving profile data: { name: '...', phone: '+7...', city: 'Якутск' }
[API] Request to /users/profile { method: 'PUT', body: '{"name":"...","phone":"+7...","city":"Якутск"}' }
[API] Response from /users/profile: { status: 200, ok: true }
[API] Success from /users/profile { id: '...', name: '...', phone: '+7...', city: 'Якутск' }
```

### 5. Проверка регистрации водителя

При регистрации как водитель:

```
[Profile] Driver registration form data: { name: '...', vehicle: '...', services: [...], legalStatus: '...' }
[Profile] Sending driver registration payload: { name: '...', vehicle: '...', services: [...], legalStatus: '...' }
[API] Request to /users/register-driver { method: 'POST', body: '...' }
[API] Response from /users/register-driver: { status: 201, ok: true }
```

### 6. Проверка backend логов

На Railway или локально:

```bash
# Просмотр логов в Railway
railway logs --service yakgo-backend

# Или локально
cd backend && npm run dev
```

Вы должны увидеть:

```
[Auth] Authenticating request: { path: '/users/profile', method: 'GET', hasInitData: true }
[Auth] Validating Telegram data
[Auth] Telegram data validated: { userId: 123456, username: '...', firstName: '...' }
[Auth] Existing user found: cm...
[Auth] Authentication successful for user: cm...
Update profile request: { name: '...', phone: '+7...', city: 'Якутск', userId: 'cm...' }
Profile updated successfully
```

## 🐛 Типичные проблемы и решения

### Проблема 1: "Номер не найден" при запросе

**Причина:** Номер телефона не добавлен в профиль Telegram

**Решение:**
1. Откройте настройки Telegram
2. Перейдите в "Настройки" → "Конфиденциальность"
3. Убедитесь что номер телефона добавлен
4. Разрешите приложениям доступ к номеру

### Проблема 2: "load failed" при сохранении профиля

**Что проверить в консоли:**
```
[API] Request to /users/profile
```

Если запрос не отправляется:
- Проверьте что переменная `NEXT_PUBLIC_API_URL` настроена
- Убедитесь что backend запущен

Если запрос отправляется но ошибка:
- Проверьте `[API] Response` - какой status code?
- Проверьте `[Auth]` логи в backend

**Возможные причины:**
- Backend не запущен
- Неправильный `NEXT_PUBLIC_API_URL`
- База данных недоступна
- Ошибка валидации Telegram данных

### Проблема 3: Регистрация водителя/продавца не работает

**Что проверить:**

1. В консоли браузера:
```
[Profile] Driver registration form data:
[API] Request to /users/register-driver
[API] Response from /users/register-driver
```

2. В backend логах:
```
[Auth] Authentication successful
Register as driver request: { name: '...', vehicle: '...', ... }
```

**Возможные ошибки:**
- `Already registered as driver` - уже зарегистрированы
- `Missing required fields` - не заполнены все поля формы
- Ошибка базы данных - проверьте что миграции применены

### Проблема 4: База данных не имеет поля city

**Симптом:**
```
Error updating profile: column "city" does not exist
```

**Решение на Railway:**
```bash
# Подключитесь к Railway CLI
railway link

# Запустите миграцию
railway run --service yakgo-backend npx prisma db push
```

## 🔍 Переменные окружения

### Frontend (Vercel)

```bash
NEXT_PUBLIC_API_URL=https://ваш-backend.railway.app/api
NEXT_PUBLIC_WS_URL=wss://ваш-backend.railway.app
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=YakGoBot
```

### Backend (Railway)

```bash
DATABASE_URL=${{Postgres.DATABASE_URL}}
PORT=3001
NODE_ENV=production
TELEGRAM_BOT_TOKEN=8518968975:AAG...
FRONTEND_URL=https://ваш-frontend.vercel.app
```

## 📝 Чеклист перед деплоем

- [ ] Все переменные окружения настроены
- [ ] Backend запущен и доступен на `/health`
- [ ] База данных PostgreSQL создана
- [ ] Миграции Prisma применены (`prisma db push`)
- [ ] Frontend собран без ошибок
- [ ] Telegram Bot настроен с WebApp URL
- [ ] Логи backend проверены - нет критических ошибок
- [ ] Консоль браузера проверена - нет ошибок сети

## 🚀 Следующие шаги

1. **Задеплойте изменения:**
   ```bash
   git add .
   git commit -m "fix: comprehensive debugging and phone number fixes"
   git push
   ```

2. **Проверьте Railway:**
   - Откройте Railway Dashboard
   - Проверьте что деплой прошел успешно
   - Откройте логи backend

3. **Проверьте Vercel:**
   - Откройте Vercel Dashboard
   - Проверьте что фронтенд собрался
   - Проверьте что переменные окружения настроены

4. **Тестирование:**
   - Откройте бота в Telegram
   - Откройте консоль (F12)
   - Проверьте все логи
   - Протестируйте:
     - Загрузку профиля
     - Запрос номера телефона
     - Сохранение профиля
     - Регистрацию водителя
     - Регистрацию продавца

## 📞 Поддержка

Если проблемы остаются:

1. Соберите логи из консоли браузера
2. Соберите логи из Railway backend
3. Проверьте статус всех сервисов:
   - Backend health check: `https://ваш-backend.railway.app/health`
   - Database connection
   - Frontend build status

Все должно работать! 🎉
