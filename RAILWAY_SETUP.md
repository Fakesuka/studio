# Настройка YakGo на Railway

Это руководство поможет развернуть YakGo на платформе Railway.

## Шаг 1: Создание проекта в Railway

1. Зайдите на [railway.app](https://railway.app) и войдите в аккаунт
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий `Fakesuka/studio`
5. Railway создаст проект

## Шаг 2: Настройка PostgreSQL

1. В вашем проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных и переменную `DATABASE_URL`

## Шаг 3: Настройка Backend сервиса

### 3.1 Создание Backend сервиса

1. В проекте нажмите **"+ New"**
2. Выберите **"GitHub Repo"**
3. Выберите ваш репозиторий
4. В настройках сервиса:
   - **Name**: `yakgo-backend`
   - **Root Directory**: `/backend`
   - **Start Command**: `npm run start`
   - **Build Command**: `npm run build`

### 3.2 Настройка переменных окружения для Backend

Перейдите в **Variables** и добавьте:

```bash
# Database (автоматически добавится из PostgreSQL сервиса)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Server
PORT=3001
NODE_ENV=production

# JWT Secret (сгенерируйте свой безопасный ключ)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Telegram Bot
TELEGRAM_BOT_TOKEN=8518968975:AAG4KDtNasfuQ6XvuyigoS3L2KyFKETdqEo
TELEGRAM_BOT_SECRET=your-telegram-bot-secret

# CORS - URL фронтенда (добавится после создания фронтенда)
FRONTEND_URL=${{yakgo-frontend.RAILWAY_PUBLIC_DOMAIN}}

# YooKassa Payment Integration
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_SECRET_KEY=your-secret-key

# 2GIS API
TWOGIS_API_KEY=ca41037...
```

### 3.3 Подключение PostgreSQL к Backend

1. В сервисе backend перейдите в **Settings** → **Service Variables**
2. Нажмите **"+ New Variable"** → **"Add Reference"**
3. Выберите PostgreSQL сервис и переменную `DATABASE_URL`

## Шаг 4: Настройка Frontend сервиса (Next.js)

### 4.1 Создание Frontend сервиса

1. В проекте нажмите **"+ New"**
2. Выберите **"GitHub Repo"**
3. Выберите ваш репозиторий
4. В настройках сервиса:
   - **Name**: `yakgo-frontend`
   - **Root Directory**: `/` (корневая директория)
   - **Build Command**: `npm run build`
   - **Start Command**: `npm run start`

### 4.2 Настройка переменных окружения для Frontend

Перейдите в **Variables** и добавьте:

```bash
# Backend API URL (после деплоя backend)
NEXT_PUBLIC_API_URL=${{yakgo-backend.RAILWAY_PUBLIC_DOMAIN}}/api

# WebSocket URL
NEXT_PUBLIC_WS_URL=wss://${{yakgo-backend.RAILWAY_PUBLIC_DOMAIN}}

# Telegram Bot
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=YakGoBot
```

## Шаг 5: Применение миграций базы данных

После деплоя backend:

1. Перейдите в сервис `yakgo-backend`
2. Откройте **Deployments** → последний деплой
3. В логах проверьте, что миграции применились:
   ```
   Prisma schema loaded from prisma/schema.prisma
   Datasource "db": PostgreSQL database
   🚀 Database pushed successfully
   ```

Если миграции не применились автоматически:

1. Откройте **Settings** → **Deploy Logs**
2. Проверьте ошибки
3. При необходимости запустите вручную через Railway CLI:
   ```bash
   railway run npx prisma db push
   ```

## Шаг 6: Настройка Telegram WebApp

После деплоя фронтенда вам нужно настроить Telegram Bot:

1. Скопируйте URL фронтенда из Railway (например, `https://yakgo-frontend.railway.app`)
2. Откройте [@BotFather](https://t.me/BotFather) в Telegram
3. Отправьте `/mybots`
4. Выберите вашего бота
5. Выберите **"Bot Settings"** → **"Menu Button"**
6. Выберите **"Configure menu button"**
7. Укажите URL: `https://yakgo-frontend.railway.app`

## Шаг 7: Проверка работы

### Backend проверка:
1. Откройте `https://your-backend.railway.app/health`
2. Должен вернуться: `{"status":"ok","message":"YakGo API is running"}`

### Frontend проверка:
1. Откройте Telegram
2. Найдите вашего бота
3. Нажмите **"Menu"** или кнопку запуска WebApp
4. Приложение должно открыться

## Шаг 8: Мониторинг и логи

1. В Railway Dashboard выберите сервис
2. Перейдите в **"Deployments"**
3. Откройте последний деплой
4. Нажмите **"View Logs"** для просмотра логов в реальном времени

## Устранение неполадок

### Проблема: Backend не запускается

**Решение:**
1. Проверьте логи деплоя
2. Убедитесь что `DATABASE_URL` настроен
3. Проверьте что все переменные окружения заданы

### Проблема: Prisma не может подключиться к базе

**Решение:**
1. Проверьте что PostgreSQL сервис запущен
2. Проверьте формат `DATABASE_URL` (должен начинаться с `postgresql://`)
3. Попробуйте передеплоить сервис

### Проблема: "load failed" при сохранении профиля

**Решение:**
1. Проверьте что backend запущен и доступен
2. Проверьте `NEXT_PUBLIC_API_URL` в frontend переменных
3. Проверьте CORS настройки в backend (`FRONTEND_URL`)

### Проблема: Миграции не применились

**Решение:**
Используйте Railway CLI для ручного запуска:

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к проекту
railway link

# Выберите backend сервис и запустите миграции
railway run --service yakgo-backend npx prisma db push
```

## Полезные команды Railway CLI

```bash
# Просмотр логов
railway logs --service yakgo-backend

# Запуск команд в контексте Railway
railway run --service yakgo-backend npm run prisma:studio

# Доступ к базе данных
railway connect Postgres
```

## Структура проекта в Railway

```
YakGo Project
├── PostgreSQL (Database)
├── yakgo-backend (Backend API + WebSocket + Telegram Bot)
└── yakgo-frontend (Next.js App)
```

## Важные заметки

1. **DATABASE_URL** автоматически устанавливается при подключении PostgreSQL
2. **Команда start** в backend уже включает `prisma generate` и `prisma db push`
3. **Telegram Bot** запускается автоматически при старте backend
4. **WebSocket** работает через тот же домен что и HTTP API
5. Railway автоматически предоставляет SSL сертификаты (HTTPS)

## Следующие шаги

После успешного деплоя:

1. Проверьте работу регистрации пользователей
2. Протестируйте создание заказов
3. Проверьте работу чата
4. Настройте YooKassa для приема платежей (обновите `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY`)
5. Настройте мониторинг и алерты в Railway

---

**Поддержка:** Если возникли вопросы, проверьте логи в Railway Dashboard или обратитесь к [документации Railway](https://docs.railway.app/)
