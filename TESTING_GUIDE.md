# Руководство по тестированию YakGo

Это руководство поможет вам протестировать и убедиться, что все компоненты YakGo работают правильно.

## 🔧 Быстрая проверка

### 1. Проверка переменных окружения

**Vercel (Frontend):**

Зайдите в Vercel Dashboard → Ваш проект → Settings → Environment Variables

Должны быть настроены:
```bash
NEXT_PUBLIC_API_URL=https://ваш-реальный-backend.railway.app/api
NEXT_PUBLIC_WS_URL=wss://ваш-реальный-backend.railway.app
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=YourBotName
```

⚠️ **ВАЖНО:** Замените `ваш-реальный-backend.railway.app` на реальный URL вашего backend из Railway!

**Railway (Backend):**

Зайдите в Railway Dashboard → Ваш проект → Backend service → Variables

Должны быть настроены:
```bash
DATABASE_URL=${{Postgres.DATABASE_URL}}
PORT=3001
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш-токен-бота
FRONTEND_URL=https://ваш-frontend.vercel.app
```

### 2. Получение URL Backend из Railway

1. Откройте Railway Dashboard
2. Выберите ваш проект
3. Кликните на backend сервис
4. Перейдите во вкладку **Settings**
5. Найдите секцию **Domains**
6. Скопируйте URL (например: `yakgo-backend-production.up.railway.app`)
7. Используйте этот URL в Vercel:
   ```
   NEXT_PUBLIC_API_URL=https://yakgo-backend-production.up.railway.app/api
   NEXT_PUBLIC_WS_URL=wss://yakgo-backend-production.up.railway.app
   ```

### 3. Тест подключения к Backend

**Вариант A: Через браузер**

Откройте в браузере:
```
https://ваш-backend.railway.app/health
```

Вы должны увидеть:
```json
{
  "status": "ok",
  "message": "YakGo API is running",
  "database": "connected",
  "userCount": 0
}
```

**Вариант B: Через командную строку**

```bash
# Установите зависимости (только первый раз)
cd /home/user/studio
npm install

# Запустите тест (замените URL на свой)
node test-api.js https://ваш-backend.railway.app
```

Вы должны увидеть:
```
✅ [Health Check] SUCCESS (200)
✅ [Detailed Health] SUCCESS (200)
🎉 Backend is running and responding correctly!
Database status: ✅ Connected
```

### 4. Тест через Telegram приложение

1. Откройте бота в Telegram
2. Добавьте `/test` к URL:
   ```
   https://ваш-frontend.vercel.app/test
   ```
3. Откройте эту страницу в боте
4. Нажимайте кнопки тестирования по порядку:
   - **Test Backend Health** - проверит подключение к backend
   - **Test Get Profile** - проверит получение профиля
   - **Test Update Profile** - проверит сохранение данных
   - **Test Driver Registration** - проверит регистрацию

## 📊 Детальное тестирование

### Test 1: Backend Health Check

**Что проверяется:**
- Backend запущен и отвечает
- База данных подключена
- Переменные окружения настроены

**Как проверить:**
```bash
curl https://ваш-backend.railway.app/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "message": "YakGo API is running",
  "timestamp": "2024-01-25T10:00:00.000Z",
  "database": "connected",
  "userCount": 1,
  "environment": {
    "DATABASE_URL": true,
    "TELEGRAM_BOT_TOKEN": true,
    "FRONTEND_URL": true,
    "PORT": "3001",
    "NODE_ENV": "production"
  }
}
```

### Test 2: Detailed Health Check

**Что проверяется:**
- Детальная статистика базы данных
- Количество пользователей, водителей, продавцов
- Использование памяти

**Как проверить:**
```bash
curl https://ваш-backend.railway.app/health/detailed
```

### Test 3: Frontend → Backend Connection

1. Откройте приложение в Telegram
2. Нажмите F12 (откроется консоль разработчика)
3. Зайдите в Профиль
4. В консоли должны появиться логи:

```
[Profile] Loading user data from API
[API] Request to /users/profile { method: 'GET', hasInitData: true }
[API] Response from /users/profile: { status: 200, ok: true }
[API] Success from /users/profile
```

Если видите:
```
[API] Exception for /users/profile: TypeError: Failed to fetch
```

**Проблема:** Frontend не может подключиться к backend.

**Решение:**
1. Проверьте `NEXT_PUBLIC_API_URL` в Vercel
2. Убедитесь что backend запущен
3. Проверьте что URL правильный (без лишних слешей)

### Test 4: Authentication Flow

**Что проверяется:**
- Telegram initData передается правильно
- Backend валидирует данные
- Пользователь создается/находится в базе

**Логи в консоли браузера:**
```
[Profile] Loading user data from API
[API] Request to /users/profile { hasInitData: true }
```

**Логи в Railway:**
```
[Auth] Authenticating request: { path: '/users/profile', method: 'GET' }
[Auth] Telegram data validated: { userId: 123456 }
[Auth] Authentication successful for user: cm...
```

Если видите:
```
[Auth] No init data provided
```

**Проблема:** Telegram initData не передается.

**Решение:**
1. Убедитесь что открываете через Telegram (не через браузер напрямую)
2. Проверьте что WebApp URL правильно настроен в BotFather
3. Проверьте логи: `[Telegram] User data:` должен показывать данные пользователя

### Test 5: Profile Update

**Как проверить:**
1. Откройте Профиль
2. Измените имя
3. Выберите город
4. Нажмите "Сохранить изменения"

**Ожидаемые логи:**
```
[Profile] Saving profile data: { name: '...', phone: '+7...', city: 'Якутск' }
[API] Request to /users/profile { method: 'PUT' }
[API] Success from /users/profile
```

**Railway логи:**
```
[Auth] Authentication successful
Update profile request: { name: '...', phone: '+7...', city: 'Якутск' }
Profile updated successfully
```

### Test 6: Driver Registration

**Как проверить:**
1. Откройте Профиль
2. Прокрутите до "Стать водителем"
3. Заполните все поля
4. Нажмите "Стать водителем"

**Ожидаемые логи (браузер):**
```
[Profile] Driver registration form data: { name: '...', vehicle: '...', services: [...] }
[API] Request to /users/register-driver
[API] Success from /users/register-driver
```

**Railway логи:**
```
Register as driver request: { name: '...', vehicle: '...', services: [...] }
Driver profile created successfully
```

### Test 7: Seller Registration

Аналогично driver registration, но для продавцов.

## 🐛 Решение проблем

### Проблема 1: "Failed to fetch" / "load failed"

**Симптомы:**
```
[API] Exception: TypeError: Failed to fetch
```

**Причины и решения:**

1. **Backend не запущен**
   - Проверьте Railway Dashboard → Deployments
   - Должен быть зеленый статус "Active"

2. **Неправильный URL в Vercel**
   - Проверьте `NEXT_PUBLIC_API_URL`
   - Должен быть: `https://your-backend.railway.app/api` (с `/api` в конце!)
   - НЕ должно быть: `http://` (только `https://`)

3. **CORS ошибка**
   - Проверьте `FRONTEND_URL` в Railway
   - Должен быть URL вашего фронтенда из Vercel
   - После изменения нужен редеплой backend

### Проблема 2: "No authorization data provided"

**Симптомы:**
```
[Auth] No init data provided
```

**Причины и решения:**

1. **Открыто не через Telegram**
   - Убедитесь что открываете через бота
   - Не открывайте URL напрямую в браузере

2. **WebApp не настроен в BotFather**
   - Откройте @BotFather
   - /mybots → выберите бота → Bot Settings → Menu Button
   - Укажите URL фронтенда

### Проблема 3: "Invalid authorization data"

**Симптомы:**
```
[Auth] Invalid Telegram data
```

**Причины и решения:**

1. **Неправильный BOT_TOKEN**
   - Проверьте `TELEGRAM_BOT_TOKEN` в Railway
   - Должен быть токен от @BotFather

2. **initData устарел**
   - Telegram initData истекает через 24 часа
   - Закройте и снова откройте бота

### Проблема 4: Database ошибки

**Симптомы:**
```
Error: column "city" does not exist
```

**Решение:**

```bash
# Подключитесь к Railway
railway link

# Запустите миграцию
railway run --service yakgo-backend npx prisma db push
```

### Проблема 5: Номер телефона не сохраняется

**Симптомы:**
- Кнопка "Запросить из Telegram" не работает
- Номер не появляется в поле

**Проверьте логи:**
```
[Profile] Requesting phone number from Telegram
[Profile] Phone not in user data
```

**Решения:**

1. **Номер не в профиле Telegram**
   - Добавьте номер в настройках Telegram
   - Settings → Edit Profile → Phone number

2. **WebApp API не поддерживается**
   - Обновите Telegram до последней версии
   - Некоторые старые версии не поддерживают `requestContact()`

## ✅ Чеклист работающей системы

- [ ] Backend отвечает на `/health` (200 OK)
- [ ] Database connection: true в `/health/detailed`
- [ ] Vercel переменные настроены с правильным URL backend
- [ ] Railway переменные настроены с правильным URL frontend
- [ ] Открытие в Telegram показывает логи `[Telegram] User data:`
- [ ] API запросы показывают `hasInitData: true`
- [ ] Backend логи показывают `[Auth] Authentication successful`
- [ ] Профиль загружается без ошибок
- [ ] Сохранение профиля работает (появляется toast "Профиль обновлен")
- [ ] Регистрация водителя работает
- [ ] Регистрация продавца работает

## 🚀 После всех тестов

Если все тесты прошли успешно:

1. ✅ Frontend и Backend подключены
2. ✅ База данных работает
3. ✅ Аутентификация через Telegram работает
4. ✅ Все API endpoints доступны
5. ✅ Пользователи могут регистрироваться и сохранять данные

**Приложение готово к использованию! 🎉**

## 📞 Если проблемы остаются

1. Соберите информацию:
   - Скриншот `/health` в браузере
   - Логи из консоли браузера (F12 → Console)
   - Логи из Railway (`railway logs --service yakgo-backend`)
   - Переменные окружения (без секретов!)

2. Проверьте:
   - URL backend содержит правильный домен
   - Все переменные окружения настроены
   - Backend не показывает ошибок в логах
   - Frontend собран без ошибок

3. Используйте тестовую страницу:
   - Откройте `/test` в приложении
   - Запустите все тесты
   - Скопируйте результаты из JSON
