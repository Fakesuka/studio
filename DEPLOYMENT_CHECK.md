# ✅ Проверка и настройка YakGo

## 🔗 Ваши URL

- **Backend (Railway):** `https://gregarious-laughter-production.up.railway.app`
- **Frontend (Vercel):** `https://studio-black-sigma.vercel.app`

## 🚨 КРИТИЧЕСКИ ВАЖНО: Настройка Vercel

### Шаг 1: Откройте Vercel Dashboard

1. Перейдите на https://vercel.com/dashboard
2. Найдите проект (studio-black-sigma или похожий)
3. Кликните на проект

### Шаг 2: Настройте переменные окружения

1. Перейдите в **Settings** (в верхнем меню)
2. Выберите **Environment Variables** (в левом меню)
3. Найдите или добавьте эти переменные для **Production**:

```bash
NEXT_PUBLIC_API_URL
```
**Значение:**
```
https://gregarious-laughter-production.up.railway.app/api
```

```bash
NEXT_PUBLIC_WS_URL
```
**Значение:**
```
wss://gregarious-laughter-production.up.railway.app
```

```bash
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME
```
**Значение:**
```
YakGoBot
```
(или ваше реальное имя бота)

### Шаг 3: Редеплой

После изменения переменных:

1. Перейдите во вкладку **Deployments**
2. Найдите последний деплой
3. Кликните **...** (три точки)
4. Выберите **Redeploy**
5. ✅ Подтвердите редеплой

⚠️ **БЕЗ РЕДЕПЛОЯ ИЗМЕНЕНИЯ НЕ ПРИМЕНЯТСЯ!**

## 📊 Проверка Backend (Railway)

### Способ 1: Через браузер

Откройте в браузере:
```
https://gregarious-laughter-production.up.railway.app/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "message": "YakGo API is running",
  "database": "connected",
  "userCount": 0
}
```

❌ **Если страница не открывается:**
1. Проверьте Railway Dashboard → Deployments
2. Backend должен быть в статусе "Active" (зеленый)
3. Проверьте логи: Railway Dashboard → Backend service → View Logs

### Способ 2: Детальная проверка

Откройте:
```
https://gregarious-laughter-production.up.railway.app/health/detailed
```

Должны увидеть:
- `database.connection: true`
- `database.users: число`
- `environment.DATABASE_URL: true`
- `environment.TELEGRAM_BOT_TOKEN: true`

## 📊 Проверка Frontend → Backend связи

### Через тестовую страницу

1. Откройте в боте Telegram:
   ```
   https://studio-black-sigma.vercel.app/test
   ```

2. Нажмите кнопку **"Test Backend Health"**

3. Проверьте результат:
   - ✅ **Если `success: true`** - Backend подключен!
   - ❌ **Если `success: false`** - Проблема с подключением

### Через консоль браузера

1. Откройте бота в Telegram
2. Нажмите F12 (откроется консоль разработчика)
3. Перейдите в любой раздел (например, Профиль)
4. В консоли должны быть логи:

**✅ Правильные логи:**
```
[API] Request to /users/profile
[API] Response from /users/profile: { status: 200, ok: true }
```

**❌ Ошибки:**
```
[API] Exception: TypeError: Failed to fetch
```
**Причина:** Frontend не может подключиться к backend
**Решение:** Проверьте `NEXT_PUBLIC_API_URL` в Vercel (см. выше)

## 🔧 Проверка Railway (Backend)

### Шаг 1: Откройте Railway Dashboard

1. Перейдите на https://railway.app
2. Найдите проект
3. Кликните на backend service

### Шаг 2: Проверьте переменные

Перейдите во вкладку **Variables**.

**Обязательные переменные:**

```bash
DATABASE_URL = ${{Postgres.DATABASE_URL}}
```
(должно быть автоматически из PostgreSQL сервиса)

```bash
PORT = 3001
```

```bash
NODE_ENV = production
```

```bash
TELEGRAM_BOT_TOKEN = ваш-токен-от-botfather
```
(формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

```bash
FRONTEND_URL = https://studio-black-sigma.vercel.app
```
⚠️ **БЕЗ СЛЕША В КОНЦЕ!**

### Шаг 3: Проверьте логи

1. Во вкладке **Deployments** кликните на последний деплой
2. Кликните **View Logs**
3. Проверьте что нет ошибок

**✅ Хорошие логи:**
```
🚀 Server is running on port 3001
Database schema pushed to database
Telegram bot started
```

**❌ Ошибки:**
```
Error: Cannot connect to database
Error: TELEGRAM_BOT_TOKEN is not defined
```

## 🧪 Полный тест системы

### 1. Backend работает?

```
✅ /health возвращает status: ok
✅ /health/detailed показывает database.connection: true
✅ В Railway логах нет ошибок
```

### 2. Frontend настроен?

```
✅ NEXT_PUBLIC_API_URL = https://gregarious-laughter-production.up.railway.app/api
✅ NEXT_PUBLIC_WS_URL = wss://gregarious-laughter-production.up.railway.app
✅ Сделан редеплой после изменения переменных
```

### 3. Frontend → Backend связь работает?

```
✅ /test страница показывает success: true при тесте health
✅ В консоли браузера (F12) видны логи [API] Request
✅ Нет ошибок "Failed to fetch"
```

### 4. Функционал работает?

Откройте приложение в Telegram:

```
✅ Профиль загружается
✅ Можно изменить имя
✅ Можно выбрать город
✅ При нажатии "Сохранить" появляется "Профиль обновлен"
✅ После перезагрузки данные сохранились
```

### 5. Регистрация работает?

```
✅ Форма "Стать водителем" заполняется
✅ При отправке появляется "Вы стали водителем!"
✅ Форма "Стать продавцом" заполняется
✅ При отправке появляется "Вы успешно зарегистрированы как продавец"
```

## 🐛 Если что-то не работает

### Проблема: Backend не отвечает на /health

**Проверьте:**
1. Railway Dashboard → Backend service → Deployments
2. Статус должен быть "Active" (зеленый)
3. Если красный - кликните и посмотрите ошибку

**Решение:**
- Если ошибка с DATABASE_URL - проверьте что PostgreSQL сервис подключен
- Если ошибка с PORT - добавьте переменную `PORT=3001`
- Если другая ошибка - посмотрите логи

### Проблема: "Failed to fetch" в приложении

**Причина:** Frontend не может подключиться к backend

**Решение:**
1. Проверьте `NEXT_PUBLIC_API_URL` в Vercel
2. Должно быть: `https://gregarious-laughter-production.up.railway.app/api`
3. НЕ должно быть: `http://` (только `https://`)
4. НЕ должно быть: двойных слешей `//api`
5. Сделайте редеплой фронтенда!

### Проблема: "No authorization data provided"

**Причина:** Открыто не через Telegram

**Решение:**
- Откройте через бота в Telegram
- Не открывайте URL напрямую в браузере

### Проблема: Данные не сохраняются

**Проверьте логи:**

**В браузере (F12 → Console):**
```
[Profile] Saving profile data: { name: '...', phone: '+7...', city: 'Якутск' }
[API] Request to /users/profile
```

**Должен быть успешный ответ:**
```
[API] Success from /users/profile
```

**Если видите ошибку:**
```
[API] Exception: Failed to fetch
```
→ Backend недоступен (см. решение выше)

```
[API] Response: { status: 401, ... }
```
→ Проблема с аутентификацией

```
[API] Response: { status: 500, ... }
```
→ Проблема на backend (проверьте Railway логи)

**В Railway логах:**
```
[Auth] Authentication successful for user: cm...
Update profile request: { name: '...', phone: '+7...', city: 'Якутск' }
Profile updated successfully
```

Если этих логов нет - запрос не доходит до backend.

## 📋 Чеклист после настройки

- [ ] Backend /health отвечает 200 OK
- [ ] Vercel переменные настроены с правильными URL
- [ ] Vercel frontend передеплоен после изменений
- [ ] Railway переменные настроены (DATABASE_URL, TELEGRAM_BOT_TOKEN, FRONTEND_URL)
- [ ] /test страница показывает успешное подключение
- [ ] В консоли браузера нет ошибок "Failed to fetch"
- [ ] Профиль загружается
- [ ] Профиль сохраняется
- [ ] Регистрация водителя работает
- [ ] Регистрация продавца работает

## 🎉 Если все чеклисты пройдены

**Приложение работает корректно!**

Все данные должны сохраняться:
- Имя пользователя
- Номер телефона
- Город
- Регистрация водителя/продавца

Если проблемы остаются - соберите:
1. Скриншот `/health`
2. Логи из консоли браузера (F12)
3. Логи из Railway
4. Скриншоты переменных окружения (без секретов!)
