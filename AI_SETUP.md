# 🤖 Настройка AI для диагностики проблем

YakGo поддерживает несколько вариантов AI для анализа проблем с автомобилем и рекомендации услуг.

## Варианты AI

### 1. 🎯 Qwen AI (Рекомендуется)

Три способа использования Qwen:

#### Вариант A: Ollama (Локально, Бесплатно) ⭐ Лучший для разработки

**Установка Ollama:**

```bash
# macOS/Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows
# Скачайте с https://ollama.com/download
```

**Установка Qwen модели:**

```bash
# Установите модель Qwen (7B - оптимальный размер)
ollama pull qwen2.5:7b

# Или версию 14B для лучшего качества (требует больше RAM)
ollama pull qwen2.5:14b
```

**Настройка в .env.local:**

```env
OLLAMA_ENABLED=true
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b
```

**Проверка работы:**

```bash
# Проверьте что Ollama запущен
ollama list

# Тест модели
ollama run qwen2.5:7b "Привет!"
```

#### Вариант B: Alibaba Cloud DashScope API (Облако)

**Получение API ключа:**

1. Зарегистрируйтесь на [DashScope](https://dashscope.aliyun.com/)
2. Создайте API ключ
3. Добавьте в `.env.local`:

```env
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
```

**Преимущества:**
- ✅ Не требует локального GPU
- ✅ Быстрая скорость
- ❌ Платно (есть бесплатный tier)

#### Вариант C: OpenAI-совместимый API

Можно использовать любой OpenAI-совместимый endpoint (включая Qwen через vLLM, LocalAI и др.)

```env
OPENAI_API_KEY=your-api-key
OPENAI_BASE_URL=https://your-endpoint.com/v1
```

### 2. Google AI (Genkit) - Текущее решение

Если хотите оставить Google AI:

```env
GOOGLE_AI_API_KEY=your-google-api-key
```

---

## 🚀 Быстрый старт с Ollama (Рекомендуется)

### Шаг 1: Установка

```bash
# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh
```

### Шаг 2: Загрузка модели

```bash
ollama pull qwen2.5:7b
```

### Шаг 3: Запуск Ollama

```bash
# Ollama запускается автоматически как сервис
# Проверка статуса:
ollama list
```

### Шаг 4: Настройка переменных

Добавьте в `.env.local`:

```env
OLLAMA_ENABLED=true
```

### Шаг 5: Тестирование

Откройте приложение, создайте заявку и опишите проблему. AI автоматически предложит услугу!

---

## 📊 Сравнение вариантов

| Вариант | Стоимость | Скорость | Качество | Требования |
|---------|-----------|----------|----------|------------|
| **Ollama** | Бесплатно | Средняя | Отличное | 8GB RAM |
| **DashScope** | Платно | Быстрая | Отличное | Интернет |
| **OpenAI** | Платно | Быстрая | Отличное | Интернет |
| **Google AI** | Платно | Быстрая | Хорошее | Интернет |
| **Fallback** | Бесплатно | Мгновенная | Базовое | Нет |

---

## 🔧 Конфигурация

### Приоритет выбора AI:

Приложение автоматически выбирает AI в следующем порядке:

1. **DashScope** (если есть `DASHSCOPE_API_KEY`)
2. **Ollama** (если `OLLAMA_ENABLED=true`)
3. **OpenAI** (если есть `OPENAI_API_KEY`)
4. **Fallback** (простое распознавание ключевых слов)

### Переменные окружения (.env.local):

```env
# Ollama (локально)
OLLAMA_ENABLED=true
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# DashScope API (облако)
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx

# OpenAI-совместимый API
OPENAI_API_KEY=your-api-key
OPENAI_BASE_URL=https://your-endpoint.com/v1

# Google AI (старое решение)
GOOGLE_AI_API_KEY=your-google-api-key
```

---

## 🧪 Тестирование

### Тест через приложение:

1. Откройте приложение
2. Создайте новую заявку
3. Введите описание проблемы, например:
   - "Машина не заводится, на улице -40"
   - "Закончился бензин на трассе"
   - "Спустило колесо"
4. AI автоматически предложит услугу

### Тест Ollama напрямую:

```bash
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:7b",
  "prompt": "Машина не заводится в мороз. Что делать?",
  "stream": false
}'
```

---

## 🐛 Troubleshooting

### Ollama не запускается

```bash
# Проверьте статус
ollama list

# Перезапустите
brew services restart ollama  # macOS
systemctl restart ollama      # Linux
```

### Модель работает медленно

- Используйте меньшую модель: `qwen2.5:3b`
- Закройте другие приложения
- Увеличьте RAM

### AI не работает

1. Проверьте переменные окружения
2. Проверьте логи в консоли браузера (F12)
3. Проверьте логи backend
4. Fallback всегда работает как запасной вариант

---

## 📚 Дополнительные ресурсы

- [Ollama Documentation](https://ollama.com/docs)
- [Qwen Models](https://ollama.com/library/qwen2.5)
- [DashScope API](https://dashscope.aliyun.com/)
- [Qwen GitHub](https://github.com/QwenLM/Qwen)

---

## 💡 Рекомендации

**Для разработки:**
- Используйте Ollama с моделью `qwen2.5:7b`
- Быстро, бесплатно, хорошее качество

**Для production:**
- Используйте DashScope API
- Стабильно, быстро, масштабируемо

**Низкие ресурсы:**
- Используйте Fallback (ключевые слова)
- Работает без AI, мгновенно
